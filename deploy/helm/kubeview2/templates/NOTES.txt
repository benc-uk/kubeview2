╭─────────────────────────────────────────────────╮
│   🌍 KubeView2 was deployed to Kubernetes! 🚀   │ 
╰─────────────────────────────────────────────────╯

📦 Image:    {{ .Values.image.repository }}:{{ .Values.image.tag }}
⚡ Replicas: {{ .Values.replicaCount }}
🌐 Service:  {{ .Values.service.type }}
🔀 Ingress:  {{ .Values.ingress.enabled }}
‎ 

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  Application is available at this URL:
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ . }}
  {{- end }}
{{- end }}
{{- else }}
  To get the URL to open KubeView2, run the following:

  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "kubeview2.fullname" . }} -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
  echo http://$SERVICE_IP:{{ .Values.service.port  }}

  NOTE: It may take a few minutes for the LoadBalancer IP to be available!
        You can watch the status of by running 'kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "kubeview2.fullname" . }}'
{{- end }}
