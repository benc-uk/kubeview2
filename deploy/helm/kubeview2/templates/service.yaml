apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeview2.fullname" . }}
  labels:
    {{- include "kubeview2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubeview2.selectorLabels" . | nindent 4 }}
